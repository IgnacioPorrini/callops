// content.js — forwards messages from injected page context to background
// The picker overlay is injected via scripting.executeScript and calls
// chrome.runtime.sendMessage directly (works in that context).
// This file is kept minimal.
